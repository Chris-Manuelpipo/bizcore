services:
  postgres:
    image: postgres:16
    container_name: bizcore-postgres
    environment:
      POSTGRES_DB: bizcore
      POSTGRES_USER: bizcore
      POSTGRES_PASSWORD: bizcore123
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:


# psql postgresql://mon_utilisateur:mon_mot_de_passe@localhost:5432/ma_base